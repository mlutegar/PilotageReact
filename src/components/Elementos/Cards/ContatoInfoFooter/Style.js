import styled from "styled-components"

export const ContatoInfoFooterStyle = styled.div`
    display: flex;
    flex-direction: row;
    color: var(--secundaria);
    max-width: 15rem;
    gap: 1rem;
    align-items: center;
    padding: 1rem 0;
    
    a {
        color: var(--secundaria);
    }
    
`

